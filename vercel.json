{
  "builds": [
    { "src": "cmd/server/main.go", "use": "@vercel/go" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/public/static/$1" },
    { "src": "/images/(.*)", "dest": "/public/images/$1" },
    { "src": "/(.*)", "dest": "/cmd/server/main.go" }
  ]
}
